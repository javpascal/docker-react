language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t javpascal/docker-react -f Dockerfile.dev .

script:
  - docker build javpascal/docker-react npm run test -- --coverage